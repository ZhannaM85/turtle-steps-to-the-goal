ext {
    // #335 — bumped 24 -> 26 for Health Connect (androidx.health.connect
    // requires API 26). No installed base yet (Play Store release, Tier
    // 36, hasn't shipped), and API 24/25 share is negligible by now — see
    // the #335 child issue for the full reasoning, confirmed with the user
    // before bumping.
    minSdkVersion = 26
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
}