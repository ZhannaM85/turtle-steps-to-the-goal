English Tesseract LSTM model (tessdata_fast) for #742 screenshot OCR.

Apache-2.0. Hosted locally so the PWA does not fetch a CDN (`connect-src 'self'`).
Loaded as `eng.traineddata` (`gzip: false` in `recognizeZeppScreenshot`).
